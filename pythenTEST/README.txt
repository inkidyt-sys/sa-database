pythenTEST/
├── main.py             # (主應用程式: DSAHelperApp, 負責 UI 和狀態管理)
├── constants.py        # (所有位址、AOB、偏移量、字典)
├── app_ui.py           # (UI 建立輔助函式)
├── ui_components.py    # (可重複使用的 UI 元件, 如 ScrollableFrame)
├── memory_worker.py    # (核心: 記憶體監控執行緒)
├── utils.py            # (通用輔助函式, 如 is_admin)
└── icon.ico            # (您原有的圖示檔案)